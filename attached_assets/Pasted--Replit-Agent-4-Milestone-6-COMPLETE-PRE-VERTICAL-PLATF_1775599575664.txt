# Replit Agent 4 — Milestone 6: COMPLETE PRE-VERTICAL PLATFORM

**Repo:** https://github.com/WebWakaDOS/webwaka-os  
**Status:** Milestone 6 — ALL REMAINING PLATFORM FOUNDATIONS  
**Branch:** `feat/milestone-6` from current `main`  
**Target:** Full vertical readiness in one comprehensive implementation

## 🎯 **3 CRITICAL PLATFORM LAYERS** (implement ALL)

### **LAYER 1: PAYMENTS (Revenue Gating)**

#### **1.1 D1 Migration 0011**
```
infra/db/migrations/0011_payments.sql
```
```sql
CREATE TABLE billing_history (
  id              TEXT PRIMARY KEY,
  workspace_id    TEXT REFERENCES workspaces(id),
  subscription_id TEXT REFERENCES subscriptions(id),
  paystack_ref    TEXT UNIQUE,
  amount_naira    INTEGER NOT NULL,
  status          TEXT NOT NULL, -- pending|success|failed|refunded
  metadata        TEXT DEFAULT '{}',
  created_at      INTEGER DEFAULT (unixepoch())
);

CREATE INDEX idx_billing_workspace ON billing_history(workspace_id);
CREATE INDEX idx_billing_status ON billing_history(status);
CREATE INDEX idx_billing_paystack_ref ON billing_history(paystack_ref);
```

#### **1.2 `packages/payments`**
```
packages/payments/src/
├── paystack.ts — initializePayment(), verifyPayment()
├── subscription-sync.ts — payment → entitlement update
├── types.ts — PaymentIntent, BillingRecord, ProviderConfig
└── index.ts
```
**Paystack secret:** `env.PAYSTACK_SECRET_KEY`

#### **1.3 API Routes**
```
POST /workspaces/:id/upgrade — create Paystack checkout
POST /payments/verify — verify webhook + sync entitlements
GET  /workspaces/:id/billing — payment history
```

### **LAYER 2: FRONTEND COMPOSITION**

#### **2.1 `packages/frontend`**
```
packages/frontend/src/
├── tenant-manifest.ts — PWA manifest generator
├── profile-renderer.ts — Hono SSR for tenant profiles
├── admin-layout.tsx — dashboard shell (minimal React)
├── discovery-page.tsx — public discovery page
└── theme.ts — tenant theming contracts
```

#### **2.2 New Apps**
```
apps/tenant-public/ — Hono SSR for tenant profiles
apps/admin-dashboard/ — React + Hono hybrid dashboard
```

#### **2.3 API Routes** 
```
GET  /public/:tenantSlug — tenant profile page
GET  /admin/:workspaceId/dashboard — admin shell
POST /themes/:tenantId — update tenant theme
```

### **LAYER 3: EVENT BUS & PROJECTIONS**

#### **3.1 D1 Migration 0012**
```
infra/db/migrations/0012_event_log.sql
```
```sql
CREATE TABLE event_log (
  id         TEXT PRIMARY KEY,
  aggregate  TEXT NOT NULL, -- entityType
  aggregate_id TEXT NOT NULL,
  event_type TEXT NOT NULL,
  tenant_id  TEXT NOT NULL,
  payload    TEXT NOT NULL, -- JSON
  version    INTEGER NOT NULL,
  created_at INTEGER DEFAULT (unixepoch())
);

CREATE UNIQUE INDEX idx_event_log_aggregate_version ON event_log(aggregate, aggregate_id, version);
CREATE INDEX idx_event_log_tenant ON event_log(tenant_id);
```

#### **3.2 `packages/events`**
```
packages/events/src/
├── event-types.ts — EntityCreated, ClaimAdvanced, PaymentSuccess, etc
├── publisher.ts — publishEvent(aggregate, event)
├── subscriber.ts — subscribe(eventType, handler)
├── projections/search.ts — rebuild search index from events
└── index.ts
```

#### **3.3 Projection Jobs**
```
apps/projections/ — Cloudflare Worker
├── src/rebuild-search-index.ts
├── src/rebuild-analytics.ts  
└── src/event-processor.ts
```

## 🚀 **IMPLEMENTATION SEQUENCE** (3 days)

```
Day 1: Payments layer (0011 + packages/payments + billing routes)
Day 2: Frontend layer (packages/frontend + tenant-public + admin-dashboard)  
Day 3: Events layer (0012 + packages/events + projections app)
```

## 📋 **EXACT DELIVERABLES** (18 items)

```
✅ 0011: billing_history table
✅ packages/payments/ — Paystack + sync
✅ POST /workspaces/:id/upgrade
✅ POST /payments/verify webhook
✅ GET /workspaces/:id/billing

✅ packages/frontend/ — PWA + SSR contracts
✅ apps/tenant-public/ — profile renderer
✅ apps/admin-dashboard/ — React shell  
✅ GET /public/:tenantSlug
✅ GET /admin/:workspaceId/dashboard

✅ 0012: event_log table
✅ packages/events/ — publisher + subscriber
✅ apps/projections/ — event processor worker
✅ Event replay: rebuild search index
✅ 50+ new tests
✅ CI 100% green
```

## 🚫 **STRICT NO-GO ZONES**
```
❌ NO vertical implementations yet
❌ NO Paystack test mode (use live keys in staging)
❌ NO full React apps (hybrid Hono+React only)
❌ NO direct main pushes
❌ NO premature commerce/transport code
```

## 📊 **TEST TARGET**
```
Current: 202 tests passing
Target: 275+ tests (202 + 75 new)
Breakdown: payments(20), frontend(25), events(30)
```

## 📝 **PR REQUIREMENTS**
```
Branch: feat/milestone-6
Title: "feat(milestone-6): payments + frontend + events — vertical readiness"
Labels: milestone-6, review-needed, base44
```

## ✅ **SUCCESS = VERTICALS UNBLOCKED**
```
✅ Tenants can pay → get entitlements
✅ Tenant-branded public profiles render
✅ Events drive projections reliably
✅ Admin dashboard shell ready
✅ 275+ tests passing
✅ Ready for commerce/transport slices
