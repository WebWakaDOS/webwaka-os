{
  "name": "@webwaka/identity",
  "version": "0.1.0",
  "description": "Nigerian identity verification — BVN, NIN, CAC, FRSC via Prembly/Paystack abstraction",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@webwaka/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "keywords": ["identity", "bvn", "nin", "cac", "frsc", "kyc", "nigeria", "prembly"]
}
